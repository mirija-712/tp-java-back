spring.application.name=auth
server.port=8080

spring.datasource.url=jdbc:mysql://localhost:3307/auth_db?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=X9!mQ@7ZA2^kP$R4&F8sLwEJ*H
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- JPA / Hibernate ---
# ddl-auto=none : on utilise schema.sql pour crùer les tables, pas Hibernate
# show-sql=false : ne pas afficher les requùtes SQL en console
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# --- Scripts SQL d'initialisation ---
# AprËs Hibernate (defer-datasource-initialization) : comptes de test dans data.sql
spring.sql.init.mode=always
spring.sql.init.data-locations=classpath:data.sql

spring.jpa.defer-datasource-initialization=true
